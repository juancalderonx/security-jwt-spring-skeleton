# JWT Standard - Spring Boot Security
This repository contains a skeleton for using the **JWT Security Standard** in my projects. It contains a "**Security**" package with all the necessary classes inside.
